let topsSongs = [
  "First time ever I saw your face",
  "God only knows",
  "A day in the life",
  "Life on mars"
];
console.log(topsSongs);

topsSongs.push("Fortunate Son"); // "pushes" the item on to the end of the array

console.log(topsSongs);

let last = topsSongs.pop(); // removes the last item from the array and returns the removed value

console.log(topsSongs);

console.log(last);
